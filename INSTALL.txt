CONSOLIDATED — every fashion + telco fix from the 17 Sep evening session, in one deploy.
Deploy THIS instead of the individual zips (fix-fashion-collections, oos-plp, hide-material-facet,
plan-cards, drop-flow-link). Everything is already merged in these files.

WHAT'S IN IT:
1. FASHION COLLECTIONS (critical): verticals.json fashion `order` -> Women/Men/Shoes/Accessories, so
   window.COLLECTIONS populates. Fixes the "all sites broken / Eureka not serving" (empty COLLECTIONS ->
   Makeup fallback -> cf=Makeup -> 0 results). category.html/cart/search/index Makeup landmines removed.
2. CONTENT NAV CONSISTENCY (store.js): telco/finance render V.content.nav into the masthead on EVERY page
   (same font, same items) instead of raw catalogue collections. This fixes topup.html showing a different
   nav / different Phones target / different font — topup uses data-collection-nav like the rest.
3. REMOVE THE REDUNDANT "START AN APPLICATION" / "TOP UP OR CHANGE PLAN" MASTHEAD LINK (vertical.js): on
   content verticals the funnel is ALWAYS entered from a specific product card (Open an account / Apply for a
   card / Apply for a loan / Choose plan), which carries the chosen product into the flow. The generic masthead
   flow link is redundant and led to an awkward "Choose account" step with the account pre-selected and the
   picker disabled — so it's removed entirely on telco + finance. Telco keeps its real "Top up" NAV item (a
   separate thing); banking nav is Checking/Savings/Credit Cards/Loans/Get a quote. flow.html is untouched
   (retail/travel still use it).
4. OOS ON PLP (store.js + build.py + verticals.json): fashion size_gaps=True + full_oos=8. PLP cards show
   "Sold out" / "Low stock" badges for the Agent One back-in-stock demo.
5. TELCO PLAN CARDS + FINANCE PRODUCT CARDS (store.js + styles.css):
   - Telco Plans: simple tariff card (name, data allowance, price/mo, "Choose plan").
   - Finance products (Accounts, Savings, Credit Cards, Loans): a CITIZENS-STYLE card — an image
     (placeholder pixel now, personalizable later), name, rate-line headline (AER/APR/fee), a short blurb,
     up to 3 key detail rows (from product_cards features), and a button that starts the funnel
     (open.html?line=<product>). Button reads "Open an account" for accounts/savings/cards, "Apply now" for loans.
   - LOANS show cards like the others (a few products, each with an Apply button into the loan flow) — NOT a
     redirect and NOT a bare grid. Image/telco-style styling can be swapped in later.
6. NO FACETS/SORT ON CONTENT VERTICALS (category.html): telco (Vantis) and finance (Northbank) are a
   company's OWN self-service app — like Citi or Bank of America. You don't filter/sort your own accounts or
   plans. So on content-template verticals the facet sidebar, the sort toolbar and the product count are all
   HIDDEN entirely (drawFacets bails for template==='content'; CSS hides .toolbar/#facets on telco/banking).
   Fashion keeps its facets but hides the exploded Material facet (HIDDEN_FACETS). This supersedes the earlier
   "hide junk telco facets" approach — the right answer is no commerce chrome at all on a bank/telco app.
7. BANKING TYPOGRAPHIC CARDS (styles.css): finance product cards render as text cards, no pixel image.

From the posh folder:
  unzip -o ~/Downloads/posh-2026-09-17-CONSOLIDATED.zip -d /tmp/x && cp -r /tmp/x/posh-2026-09-17-CONSOLIDATED/* . && python3 build.py --all && posh "Consolidated 17 Sep: fashion collections, content nav on all pages, drop flow link, OOS PLP, plan cards, hide material facet, banking cards"

STILL OPEN (not in this zip, separate follow-ups):
- Telco product imagery is wrong (generic phone+laptop+giftwrap stock in assets/img/telco/). Plans are now
  imageless so plan search looks fine; telco PHONE cards still show the bad stock. Fix = regenerate telco
  images (add a telco block to photos.py + Unsplash key) OR give telco the same no-image card treatment.
- Telco Phones one-column: the telco image files 404 (assets/img/telco/ empty in the build) -> broken images
  collapse the grid. Same root as the imagery issue above.
- search.html still has beauty leftovers ("makeup collection" link, "Lumen" title/footer) on non-beauty verticals.
- material-facet.zip (clean the data + reindex) is the ALTERNATIVE to hiding it — not included; hiding wins for now.
Same-name-download trap: if 'nothing to commit', unzip the newest per ls -lt.

8. QUOTE = CAPTURE-AND-FOLLOW-UP, NOT INSTANT PRICE (quote.html): a real insurance quote request usually
   captures your details and hands off to underwriting/an adviser — it doesn't hand back a final price on the
   spot. quote.html now stores the request (quote_status:'Requested', customer_status:'Quote requested') and
   fires a `quote_requested` event, then confirms "your quote request is in — a specialist will send your
   personalised quote by email shortly" (an indicative estimate is shown, marked subject to the final quote).
   THAT submission is the trigger: an Architect journey listens for `quote_requested` to email the quote or
   book an adviser callback — the demo point (a form submit kicks off orchestration on the customer's side),
   mirroring the loan flow's eligibility -> decision -> banker handoff.

9. NORTHBANK ACCOUNT TIER (sources/banking-1.json + verticals.json): the Accounts page had a single lonely
   "Everyday Current Account". Now it's a proper tier of four, with plain names (no odd product names):
   - Basic — no fees, no overdraft, getting-started account.
   - Everyday — the standard day-to-day account (renamed from "Everyday Current Account").
   - Premium — €12/mo, travel insurance, cashback on bills, higher limits.
   - Family — €8/mo, joint & child accounts, shared pots, kid spending controls.
   Each renders as a Citizens-style card (rate line + features + "Open an account" -> funnel). Savings keeps
   its easy-access + fixed pair. Stale "Everyday Current Account" references (popular card, open-funnel types
   list) were repointed to "Everyday".
   NOTE: rebuild banking after unzip (already included the rebuilt catalog, but build.py --all is in the deploy
   command so it regenerates cleanly).

10. SINGLE INSURANCE BRAND — NO MORE CALDER/ARDENT/FAIRHAVEN (sources/insurance-1.json + verticals.json +
    generate.py): insurance was a multi-brand comparison (Northbank Insure, Fairhaven, Ardent Cover, Calder
    Protect) which put odd invented brand names on the cards. Collapsed to a SINGLE clean brand: Northbank.
    Every product is now "Northbank <Cover> Insurance" with its cover tiers intact (Third Party / TPFT /
    Comprehensive, Buildings / Contents / Both, etc.). Exact brand-merge duplicates removed (20 -> 10 products).
    verticals.json insurance.brand: Fairhaven -> Northbank (build.py stamps vendor from this). generate.py
    BANK_BRANDS/INS_BRANDS collapsed to ["Northbank"] so the odd names can't regenerate.
    The structure you liked (loans/cards with cover tiers) is unchanged — just one brand now.

11. CHECKING vs SAVINGS (sources/banking-1.json + verticals.json): "Accounts" and "Savings" were redundant
    (both are accounts). Renamed the Accounts collection to CHECKING — so the finance nav is now
    Checking / Savings / Credit Cards / Loans, matching how real banks label it. The four current accounts
    (Everyday/Basic/Premium/Family) live under Checking; Savings keeps easy-access + fixed. Nav, order,
    collections map, hero_category and subcats updated; generic prose ("open an account in minutes") left as-is.

12. RICHER FINANCE PRODUCT CONTENT (verticals.json + store.js): every finance product now has a fuller
    2-sentence description and 6-7 detailed feature rows (rates, terms, eligibility, FSCS protection) — the
    savings-page level of detail applied across Checking, Savings, Credit Cards and Loans. The finance card
    shows up to 6 features (was 3) so the detail surfaces. e.g. Personal Loan spells out soft-search rate
    check, fixed payments, £1k-£25k, 1-7yr terms, rep 6.9% APR; Rewards Card spells out cashback, no annual
    fee, 0% 12mo, fraud tools, rep 24.9% APR variable.

13. NO CART ON TELCO/FINANCE (vertical.js): a self-service telco/bank app has no shopping cart — you apply
    for a plan/account, you don't add it to a basket. The masthead Cart link (which led to an empty, weird
    cart.html) is now REMOVED entirely on content-template verticals. Retail/travel keep their cart.

14. TELCO NAV — DROP DEAD HELP (verticals.json): the telco "Help" nav link had href='#' (led nowhere). No
    help/support page exists, so it's removed — telco nav is now Plans / Phones / Top up. (Build a real Help
    page later if wanted.) The "Phones has no menu" issue is the content-nav fix in #2 — not yet deployed on
    the sandbox; deploying this zip fixes it (inner pages get the same masthead nav as the home).

15. BANKING HOMEPAGE — CURATED + WIDER CARDS + RIGHT CTAs (verticals.json + content-index.html + styles.css):
    - The homepage showed ALL 11 products (too much). Now it shows a curated 4 — Everyday, Northbank Saver,
      Rewards Card, Personal Loan — one representative per family (content.homepage_products). No plan clutter.
    - The homepage cards used a single generic "Open account" CTA (the linecard renderer, separate from the
      category card--finance). Now per-collection: Credit Cards -> "Apply for a card", Loans -> "Apply for a
      loan", Checking/Savings -> "Open an account". (This is why the Credit Card card said "Open account" on
      the HOME page even after the category-card fix — two different renderers.)
    - Cards widened (minmax 340px) so a curated set of 4 spreads across the page; Checking, Savings and the
      homepage cards are now the same, wider size.

16. BANKING CARD POLISH (verticals.json + styles.css):
    - Homepage: dropped the loan from the curated set -> 3 cards (Everyday, Saver, Rewards Card) that fit one
      row cleanly, no wrap to a second row.
    - Removed OBSOLETE banking .card overrides that predated card--finance and were double-styling the cards
      (likely why Credit Cards / Loans looked different from Checking/Savings — the conflicting rule is gone).
    - Card widths: min 240px (so 3-4 fit one row without wrapping) + a 360px MAX cap and left-align, so a page
      with only 2 cards (Savings) doesn't stretch them absurdly wide. Checking/Savings now consistent, sensible width.
    NOTE: Credit Cards & Loans use the SAME card--finance renderer as Checking/Savings, keyed on the banking
    vertical (collection-agnostic), and their names match the product_cards config (blurb+features present). If
    they STILL look different after deploy, it's a render detail to inspect live — the data and renderer are correct.

17. TELCO PHONES GETS FACETS BACK — CLEAN ONES (category.html): the "no facets on content verticals" rule was
    too broad. Phones IS a real product catalogue (a phone shop), unlike plans/accounts. So the Phones page now
    shows facets again — but CLEAN ones built locally, not Eureka's junk (marketing colour names / model
    product_types were all count-1): a BRAND facet (Cobalt / Northpoint / Ridgeway / Aether / Vantis, from the
    name) and a PRICE facet (Under €50/€100/€200/Any), with client-side filtering. Plans / account / finance
    pages stay facet-free (bank-app model). Scoped via data-phones on <html> so only the Phones page shows them.

18. FINANCE CARD WIDTH — FIXED, NOT STRETCHY (styles.css): the auto-fit grid stretched columns to fill the
    page while the card capped at 360px, so 2 Savings cards floated as capped cards inside half-page-wide
    columns (looked off). Switched to FIXED 320px columns that pack from the left — 2 cards now look the same
    as 4, neatly side by side, no floating-in-a-gap. Applies to homepage linecards and category card--finance.

19. FINANCE CARD DATA TRAVELS WITH THE PRODUCT (build.py + store.js): finance card rate-line/blurb/features
    were looked up from content.product_cards by NAME at render time — if that lookup missed (e.g. Eureka
    returns a slightly different product name), the card lost its rate line + features and rendered bare and
    different from the others. This was almost certainly why Credit Cards looked different / had no descriptive
    text. FIX: build.py now stamps rate_line/blurb/features onto each content-vertical product record from the
    config, and the card reads p.rate_line/p.blurb/p.features as a fallback — so the card is fully populated
    regardless of the name-match or whether Eureka or local served it. Rebuild banking (in the deploy command).

20. ACCOUNT PAGE DE-CLUTTER (account.html):
    - Removed the weird standalone "Explore products" button from the finance account view — a logged-in
      customer isn't "exploring products" from their own account. (Telco keeps "Top up or change plan", which
      IS a natural account action.)
    - Moved the persona AUTHORING tools (Save current profile as a persona, Copy my personas as JSON, the
      "give this profile a history" link) into a collapsed <details> "Demo tools (SC only)" disclosure, so the
      customer-facing view (balance, recent activity, offers, relationship) isn't cluttered with SC machinery.
      Persona sign-in cards stay visible (that's the SC's way to switch profiles).
    STILL A BIGGER FOLLOW-UP: fully splitting the account page into a clean customer view + a separate SC/demo
    page (personas, mimic controls, save-as-persona) is a larger restructure — deferred; this pass removes the
    worst of the clutter. The offer/outreach "ideas" cards are left as placeholders per your call.

21. LOANS/CREDIT CARDS SVGs — BROADER CSS BACKSTOP (styles.css): confirmed the deploy is live (Northbank shows)
    but Loans still showed SVGs — so it's a real render-path difference, NOT stale code. The main card() finance
    branch is verified correct and fires for all banking products, so the SVGs come from a DIFFERENT renderer
    (most likely the Smart Recommender "You might also need" widget, which builds its own image cards). Broadened
    the banking SVG-hide CSS to cover .ins-web-smart-recommender-box-item images too, so stray product images are
    hidden anywhere on banking except the intentional finance-card media.
    HONEST NOTE: this is a blind fix (couldn't see the rendered DOM). If Loans STILL shows SVGs after this,
    inspect ONE svg in the browser: what's its parent element's class? That names the exact renderer and I can
    target it precisely. The main product card is correct; this is about a secondary widget showing images.
