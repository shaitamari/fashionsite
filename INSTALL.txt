Account restructure (telco + finance): real top-up funnel, clean customer view, "Mimic usage" SC control.

- topup.html: REAL top-up/change-plan funnel (Add data boost / Change plan / Top up credit) -> pick -> confirm.
  Replaces the retired flow.html services form. Telco nav gains "Top up".
- account.html: fake "Use 2 GB"/"Make £50" customer buttons GONE. Clean usage/balance view + a real
  "Top up or change plan" button -> the funnel.
- The usage bar reflects the SITE'S USAGE HISTORY (per-uuid). An SC-only collapsed panel labelled
  "Mimic plan usage" builds a realistic month (A light day +1GB / A typical week +6GB / A heavy month to 90% / Reset),
  so the bar + activity fill in and crossing 85% fires the upgrade nudge. Finance: "Mimic account activity".

From the posh folder:
  unzip -o ~/Downloads/posh-2026-09-17-accounts.zip -d /tmp/x && cp -r /tmp/x/posh-2026-09-17-accounts/* . && python3 build.py --all && posh "Account restructure: real top-up funnel, clean dashboard, Mimic-usage SC control"

TEST: telco account (signed-in subscriber) -> clean usage view + Top up button (-> funnel).
      Expand "Mimic plan usage" -> click "A heavy month" -> bar fills, nudge appears, activity logs.
