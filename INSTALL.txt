Per-uuid history fix (+ Makeup fallback + vertical-scoped purchases/bookings).
Files: assets/store.js, history.html
From the posh folder:
  unzip -o ~/Downloads/posh-2026-09-17-a.zip -d /tmp/x && cp -r /tmp/x/posh-2026-09-17-a/* . && posh "History per uuid; new identities never inherit prior trips/purchases"
