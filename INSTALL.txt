SUBSCRIBE FUNNEL — multi-step: plan -> number -> details -> confirm.
Overwrites 4 files. From ~/Desktop/posh:
  unzip -o ~/Downloads/telco-subscribe.zip -d /tmp/x && cp -r /tmp/x/telco-subscribe/* . && posh "Telco subscribe funnel"
No build needed (static pages + css).
PANEL: declare custom attributes plan, plan_price, subscription_status in
Attributes & Events, or Upsert will drop them (they still show on the page from signIn).
