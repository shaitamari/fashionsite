Out-of-stock products on fashion, VISIBLE ON THE PLP — for the Agent One back-in-stock demo.

- fashion size_gaps=True (one size marked out on ~a quarter of multi-size styles) + full_oos=8
  (8 whole styles marked sold out, spread across Women/Men/Shoes/Accessories, deterministic by id).
- PLP card now shows a badge: "Sold out" (whole product gone, image dimmed) or "Low stock" (a size is out).
  oneVariantEach aggregates stock across variants so the card is truthful even though it shows one variant.
- Result on a fashion category page: some cards show "Sold out" / "Low stock" -> click in -> the PDP has the
  gone size -> Agent One captures the back-in-stock request. The capability now reads from the grid.

From the posh folder:
  unzip -o ~/Downloads/posh-2026-09-17-oos-plp.zip -d /tmp/x && cp -r /tmp/x/posh-2026-09-17-oos-plp/* . && python3 build.py fashion && posh "OOS products on fashion PLP for the back-in-stock demo"

After deploy, a fashion category (Women/Men/Shoes) shows Sold out / Low stock cards. 8 fully-OOS styles incl.:
Garrison Belt, Babydoll Tank in White, Two Button Henley, and several Vans/shoes.
Same-name-download trap: if 'nothing to commit', unzip the newest per ls -lt.
