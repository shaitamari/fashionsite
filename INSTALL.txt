Fix the fashion Material facet exploding into 150+ combined values.

CAUSE: material was comma-joined ("Cotton, Denim, Lycra"), so every combination became its own facet value.
FIX (build.py): the facet field `material` is now the PRIMARY material only (first before the comma) ->
158 values collapse to ~35 clean ones (Cotton, Leather, Silk, Wool, Linen...). The full string is kept as
`material_full` for the PDP if needed.

From the posh folder:
  unzip -o ~/Downloads/posh-2026-09-17-material-facet.zip -d /tmp/x && cp -r /tmp/x/posh-2026-09-17-material-facet/* . && python3 build.py --all && posh "Material facet: primary material only (no combination explosion)"

NOTE — the LIVE facet is served by EUREKA off the feed, so the clean values appear once the feed re-syncs
and Eureka reindexes (the local-catalogue fallback facet is clean immediately). If material is a searchable
attribute/facet configured on the en_GB locale, a reindex is needed for the facet values to refresh.
Same-name-download trap: if 'nothing to commit', unzip the newest per ls -lt.
