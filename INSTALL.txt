Telco/finance nav consistency + finance typographic cards.

NAV (telco + finance): the high-level content nav (Plans/Phones/Help/Top up · Accounts/Savings/... etc.)
now shows on EVERY page in the masthead, same font everywhere — not the homepage nav on the home and the
raw catalogue collections (Modern Folio iPhone, iPhone 17 Pro Max...) in a different font elsewhere.
- Content-template verticals render V.content.nav into the masthead nav on all pages (store.js).
- Dead links (Help = '#') are skipped; anchor links (#plans) resolve to the content home from any page.
- Phones and every inner page now carry the same nav.

FINANCE CARDS: the family pages (Accounts/Savings/Credit Cards/Loans) no longer show the pixel/placeholder
image — banking product cards now read as clean TYPOGRAPHIC cards like the telco plan cards (name + rate line),
scoped to banking only (html[data-vertical="banking"]). No other vertical changes.
NOTE: this is the "make it look like my plans" option. If you'd rather have REAL images later, that's a
separate step (source real account/card imagery + drop the pixel placeholders).

From the posh folder:
  unzip -o ~/Downloads/posh-2026-09-17-nav-cards.zip -d /tmp/x && cp -r /tmp/x/posh-2026-09-17-nav-cards/* . && python3 build.py --all && posh "Telco/finance nav consistent across pages; finance typographic cards"
Same-name-download trap: if 'nothing to commit', unzip the newest per ls -lt.
