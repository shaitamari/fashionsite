Phones = a real all-devices page (was landing on one model / empty).
- Phones nav -> category.html?c=Phones
- category.html renders the whole device catalogue (minus Plans) with search + facets, clean "<n> devices" label.
1) unzip -o ~/Downloads/telco-phones.zip -d /tmp/x && cp -r /tmp/x/telco-content/* .
2) posh "Telco Phones: real all-devices page"
(no build needed — html/json only; but a build won't hurt)
