Fix fashion (Ashford Lane) category nav — was collapsing to "Tops".
Cause: the Çakıl search-team catalog swap changed product_types to Women's/Men's/Shoes/
Accessories, but verticals.json still had the OLD Posh collections map (Tops/Maxi Dresses/…),
so almost nothing matched and everything fell into Tops.
Fix: fashion `collections` remapped to the real product_types -> Women / Men / Shoes / Accessories.

From the posh folder:
  unzip -o ~/Downloads/posh-2026-09-17-e.zip -d /tmp/x && cp -r /tmp/x/posh-2026-09-17-e/* . && python3 build.py fashion && posh "Fix fashion nav: collections -> Women/Men/Shoes/Accessories"
