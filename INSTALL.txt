FIX: applyVertical was overwriting the content hero with the old product-home copy.
Guarded now — on content verticals it no longer sets hero title/lede/cta/tiles.
1) unzip -o ~/Downloads/telco-hero-fix.zip -d /tmp/x && cp -r /tmp/x/telco-content/* .
2) posh "Content verticals: applyVertical no longer overwrites the content hero"
No build needed (JS only). Hard-reload telco-sandbox after deploy.
