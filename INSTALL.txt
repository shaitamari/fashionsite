Top-up page: data-focused + realistic one-off boost framing.
- Vantis plans have unlimited minutes -> no credit/minutes (removed).
- "Add a one-off data boost": clearly one-off, kicks in when your allowance runs out, valid this billing month.
  (Matches how real UK contract data add-ons work: one-time, expiring, not PAYG-only.)
- "Change my plan": clearly a permanent bigger monthly allowance — the distinct alternative.
- Context leads with data usage + GB left + red nudge at 85%.

Single file. From the posh folder:
  unzip -o ~/Downloads/posh-2026-09-17-topup-data.zip -d /tmp/x && cp -r /tmp/x/posh-2026-09-17-topup-data/* . && posh "Top-up: data-focused, one-off boost vs permanent plan change"
