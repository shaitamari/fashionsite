FIX: Phones nav link now lands on a populated device page (was going nowhere).
Also fixed category.html's dead "Makeup" default so a bare category.html can't break.
1) unzip -o ~/Downloads/telco-phones-fix.zip -d /tmp/x && cp -r /tmp/x/telco-content/* .
2) python3 build.py telco
3) posh "Telco Phones nav lands on the device grid; category default fixed"
